"use strict"


function longestConsec(strarr, k) {
    let result = strarr[0];
  
    if (strarr.length == 0 || k > strarr.length || k <= 0) {
        return ""
    }

    for (let i = 0; i < strarr.length; i++) {
        
    }
    
}

let test = ['nanm', 'qiwqwoowowo', 'qw', 'efekkk']